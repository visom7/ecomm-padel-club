# MongoDB URI — use host.docker.internal to connect to MongoDB on the host machine
MONGO_URI=mongodb://host.docker.internal:27017/padel

# Admin PIN — change this before deploying!
ADMIN_PIN=changeme

# CORS allowed origins (comma-separated if needed)
CORS_ORIGIN=http://localhost
